$('#user_questions').html("<%= escape_javascript(render partial: 'questions/tab', locals: { questions: @questions }) %>");
$('#user_answers').html("<%= escape_javascript(render partial: 'questions/tab', locals: { questions: @answered_questions }) %>");
$('#user_posts').html("<%= escape_javascript(render partial: 'posts/tab', locals: { posts: @posts }) %>");
